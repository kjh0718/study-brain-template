# CLAUDE

이 저장소는 **Study Brain**이다. 코드 저장소가 아니라 학습 지식 저장소다.

1. [`SECOND-BRAIN.md`](SECOND-BRAIN.md)를 처음부터 끝까지 읽고 시작한다.
2. 공통 규약은 [`AGENTS.md`](AGENTS.md)에 있다. 이 파일은 그것을 복제하지 않는다.
3. 아래는 Claude Code에서만 다른 내용이다.

## 문서 우선순위

Study Brain의 규칙에 관해서는 **`SECOND-BRAIN.md`가 이 파일과 전역 `~/.claude/CLAUDE.md`보다 우선한다.** 전역 지침의 코딩·검증 규칙은 이 저장소에서 대부분 해당되지 않는다. 다루는 대상이 소스 코드가 아니라 학습 노트이기 때문이다.

충돌하면 `SECOND-BRAIN.md`를 따르고, 무엇을 왜 다르게 했는지 보고한다.

## 도구 사용

- **frontmatter 우선 검색.** Grep으로 `^(id|type|status|course|topics):` 같은 패턴을 먼저 걸어 후보를 좁히고, 그다음에만 Read로 본문을 연다. 저장소가 작아 보여도 전체를 Read하지 않는다. 레이어별 검색 명령은 [`_system/workflows/README.md`](_system/workflows/README.md)의 공통 검색 명령에 있다.
- **Edit을 쓴다.** 노트를 고칠 때 Write로 파일 전체를 다시 쓰지 않는다. 전체 재작성은 보호 영역과 자동 관리 영역의 경계를 지우는 가장 흔한 방법이다. 새 노트를 만들 때만 Write를 쓴다.
- **Bash로 노트를 수정하지 않는다.** `sed -i` 같은 일괄 치환은 문맥을 보지 않으므로 보호 영역과 원본을 함께 바꿀 수 있다. 검색과 확인에는 Bash를 써도 된다.
- `raw/` 아래 파일에는 어떤 쓰기 도구도 쓰지 않는다. 원본 보존이 목적인 폴더다.

## 병렬 처리

한 번의 입력을 처리할 때 레이어 순서가 정해져 있다. `SECOND-BRAIN.md`의 `L1의 후속 호출 순서`를 지키고, 같은 노트를 건드리는 작업을 동시에 실행하지 않는다. 서로 다른 노트를 읽는 조회는 병렬로 해도 된다.

## Skills와 Hooks

`.claude/skills/`와 `.claude/hooks/`는 **아직 비어 있다.** 스킬이 있다고 가정하고 호출하지 않는다. 앞으로 만들 스킬도 규칙을 다시 정의하지 않고 `SECOND-BRAIN.md`와 해당 워크플로 문서를 읽어 실행하는 얇은 진입점으로 둔다.

## Git

커밋과 푸시는 사용자가 명시적으로 요청할 때만 한다. 요청받지 않았으면 변경만 남기고 상태를 보고한다.
