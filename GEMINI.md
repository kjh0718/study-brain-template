# GEMINI

이 저장소는 **Study Brain**이다. 코드 저장소가 아니라 학습 지식 저장소다.

1. [`SECOND-BRAIN.md`](SECOND-BRAIN.md)를 처음부터 끝까지 읽고 시작한다.
2. 공통 규약은 [`AGENTS.md`](AGENTS.md)에 있다. 이 파일은 그것을 복제하지 않는다.
3. 아래는 Gemini에서만 다른 내용이다.

## 문서 우선순위

Study Brain의 규칙에 관해서는 **`SECOND-BRAIN.md`가 이 파일과 상위 설정보다 우선한다.** 충돌하면 `SECOND-BRAIN.md`를 따르고 무엇을 왜 다르게 했는지 보고한다.

## 긴 컨텍스트를 이유로 전체를 읽지 않는다

저장소 전체를 한 번에 넣을 수 있더라도 그렇게 하지 않는다. 이 저장소의 검색 규칙은 컨텍스트 한계 때문이 아니라 **결정적 검색**을 위한 것이다.

- frontmatter로 후보를 좁힌 뒤 남은 후보의 본문만 읽는다.
- `raw/` 원본은 `SECOND-BRAIN.md`의 L6이 정한 조건일 때만 연다. 전사 전체를 매번 컨텍스트에 넣으면 어느 근거로 답했는지 추적할 수 없다.
- 답변의 각 진술에는 근거 노트 ID를 붙인다. 컨텍스트에 있었다는 것은 근거가 아니다.

전체를 읽어야만 판단할 수 있는 작업은 L8의 무결성 검사뿐이며, 그것도 사용자가 요청했을 때만 한다.

## 전사와 자료를 요약으로 대체하지 않는다

긴 원문을 다룰 때 요약본을 만들어 원본 대신 저장하지 않는다. 요약은 구조화 노트의 본문에 들어가고, 원본은 `raw/`에 그대로 남는다.

## 파일 쓰기

- 노트를 고칠 때 파일 전체를 다시 생성하지 않는다. 필요한 부분만 고친다. 전체 재생성은 보호 영역(`## My Notes` 등)과 자동 관리 영역의 경계를 지운다.
- `raw/` 아래 파일은 쓰기 대상이 아니다.
- 한 번에 여러 노트를 일괄 변환하지 않는다. `SECOND-BRAIN.md`의 안전 순서를 따라 되돌리기 쉬운 것부터 한다.

## Skills와 Hooks

`.agents/skills/`에 Core Skill 8개의 정본이 있다. `.agents/hooks/`는 **아직 비어 있다.** 훅이 있다고 가정하지 않는다.

## Git

커밋과 푸시는 사용자가 명시적으로 요청할 때만 한다.
